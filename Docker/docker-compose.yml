version: '3.8'
services:
  web:
    build: ./nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/index.php:/usr/share/nginx/html/index.php
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/fastcgi-php.conf:/etc/nginx/snippets/

    depends_on:
      - db
    networks:
      - app-network

  db:
    build: ./mysql
    environment:
      MYSQL_DATABASE: 'InventoryDB'
      MYSQL_USER: 'tsrlearning'
      MYSQL_PASSWORD: 'tsrlearning'
      MYSQL_ROOT_PASSWORD: 'tsrlearning'
    ports:
      - "3306:3306"
    networks:
      - app-network
    volumes:
      - db_data:/var/lib/mysql
      - ./mysql/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
networks:
  app-network:
    driver: bridge

volumes:
  db_data:
